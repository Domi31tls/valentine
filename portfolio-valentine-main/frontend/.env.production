# Frontend - Variables d'environnement PRODUCTION
# Ces variables sont utilisées lors du build pour la production

# API URL - laisser vide pour utiliser le même domaine
# PUBLIC_API_URL=

# Avec Nginx qui proxy /api vers le backend, pas besoin de définir PUBLIC_API_URL
# Le frontend builded utilisera /api qui sera proxyfié par Nginx